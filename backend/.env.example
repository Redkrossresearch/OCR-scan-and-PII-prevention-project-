# Copy this file to backend/.env and replace the values with your own.
# Do not commit backend/.env to version control.

SECRET_KEY=put_a_random_long_secret_string_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
DATABASE_URL=postgresql://<username>:<password>@<host>/<database_name>?sslmode=require
